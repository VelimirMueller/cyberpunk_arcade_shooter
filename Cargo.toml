[package]
name = "cyberpunk_rpg"
version = "0.1.0"
edition = "2024"

[features]
default = []
webgpu = ["bevy/webgpu"]

[dependencies]
bevy = "0.16.1"
rand = "0.8"

[target.'cfg(target_arch = "wasm32")'.dependencies]
getrandom = { version = "0.2", features = ["js"] }
